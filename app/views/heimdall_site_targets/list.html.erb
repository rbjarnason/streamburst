<h1>Listing heimdall_site_targets</h1>

<table>
  <tr>
  <% for column in HeimdallSiteTarget.content_columns %>
    <th><%= column.human_name %></th>
  <% end %>
  </tr>
  
<% for heimdall_site_target in @heimdall_site_targets %>
  <tr>
  <% for column in HeimdallSiteTarget.content_columns %>
    <td><%=h heimdall_site_target.send(column.name) %></td>
  <% end %>
    <td><%= link_to 'Show', :action => 'show', :id => heimdall_site_target %></td>
    <td><%= link_to 'Edit', :action => 'edit', :id => heimdall_site_target %></td>
    <td><%= link_to 'Destroy', { :action => 'destroy', :id => heimdall_site_target }, :confirm => 'Are you sure?', :method => :post %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Previous page', { :page => @heimdall_site_target_pages.current.previous } if @heimdall_site_target_pages.current.previous %>
<%= link_to 'Next page', { :page => @heimdall_site_target_pages.current.next } if @heimdall_site_target_pages.current.next %> 

<br />

<%= link_to 'New heimdall_site_target', :action => 'new' %>
