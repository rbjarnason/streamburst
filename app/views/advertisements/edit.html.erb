<h1>Editing advertisement</h1>

<%= form_tag( { :action => 'update', :id => @advertisement }, { :multipart => true }) %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Edit' %>
</form>

<%= link_to 'Show', :action => 'show', :id => @advertisement %> |
<%= link_to 'Back', :action => 'list' %>
