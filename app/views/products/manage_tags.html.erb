<h1>Categories update for <%= @product.title %></h1>

<%= render :partial => 'list_tags' %>

<%= form_tag( { :action => 'manage_tags' } ) %>

<p><label for="tags">Add Tags (use , as seperator)</label><br/>
<input id="product_tags" name="product[tags]" size="30" type="text" value="" /></p>

  <%= submit_tag "Add" %>
</form>
<%= link_to 'Back', :action => 'list' %>
